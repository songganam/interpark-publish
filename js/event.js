window.addEventListener("load", function () {
  console.log("추천상품코딩");
  // 추천 상품 슬라이드 기능
  // 글로써 코딩 시나리오 작성 : 의사코드
  // 1. 외부 데이터를 불러온다.

  // 외부 데이트 가져올때 작성법
  const xhr = new XMLHttpRequest();

  // 2. html 태그를 백틱을 이용해서 만든다.
  const htmEventTag = ``;

  // 3. swiper 태그에 백틱을 배치한다.
  const eventSlide = ".event-slide .slide-wrapper";

  // 4. swiper 작동시킨다.
  //   const eventSwiper = new Swiper(".event-slide");
});
